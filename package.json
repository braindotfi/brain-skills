{
  "name": "brain-finance-plugin",
  "version": "0.1.0-beta.1",
  "private": true,
  "description": "Distribution and validation scripts for the Brain Finance agent plugin.",
  "type": "module",
  "scripts": {
    "check:drift": "node scripts/check-drift.mjs",
    "check:invariants": "node scripts/check-invariants.mjs",
    "check:references": "node scripts/check-references.mjs",
    "build:agensi": "node scripts/build-agensi.mjs",
    "build:portable": "node scripts/build-portable.mjs",
    "test:package": "node scripts/smoke-plugin.mjs",
    "test:agensi": "npm run build:agensi",
    "test:portable": "npm run build:portable",
    "test:install": "node scripts/smoke-install.mjs",
    "test:live": "node scripts/smoke-live-mcp.mjs",
    "verify:phase0": "node scripts/verify-phase0.mjs",
    "test": "npm run check:drift && npm run check:references && npm run check:invariants && npm run test:package && npm run test:agensi && npm run test:portable && npm run test:install && npm run test:live"
  },
  "engines": {
    "node": ">=20"
  }
}
